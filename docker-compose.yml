services:
  db:
    image: postgres:18
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: resume_store
    ports:
      - "5555:5432"
    volumes:
      - pgdata:/var/lib/postgresql
      
volumes:
  pgdata: